services:
  - type: web
    name: altseason-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python altseason_bot.py
    envVars:
      - key: PORT
        value: 8080
